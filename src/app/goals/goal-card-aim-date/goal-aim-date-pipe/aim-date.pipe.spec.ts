/* tslint:disable:no-unused-variable */

import { GoalAimDatePipe } from './aim-date.pipe';

describe('Pipe: AimDatee', () => {
  it('create an instance', () => {
    let pipe = new GoalAimDatePipe();
    expect(pipe).toBeTruthy();
  });
});
