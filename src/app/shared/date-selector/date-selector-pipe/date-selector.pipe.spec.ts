/* tslint:disable:no-unused-variable */

import { DateSelectorPipe } from './date-selector.pipe';

describe('Pipe: AimDatee', () => {
  it('create an instance', () => {
    let pipe = new DateSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
